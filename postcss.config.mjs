/** PostCSS config for Tailwind v4 (DL-40). The Tailwind plugin lives in its own
 * package in v4; import/vendor-prefixing are handled internally. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
